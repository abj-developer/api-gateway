spring:
  datasource:
    url: jdbc:mysql://localhost:3306/api_gatewaydb_test
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: create-drop

eureka:
  client:
    enabled: false